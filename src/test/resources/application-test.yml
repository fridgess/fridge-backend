spring:
  config:
    import: optional:file:.env.test[.properties]

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  test:
    database:
      replace: none
logging:
  level:
    com:
      fridge:
        fridgebackend: debug
    org:
      hibernate:
        sql: debug
